/**
 *
 * Model Exports
 *

 * @author      :: Isom Durm (isom@phenomapp.com)
 *
 * Exposes global model configuration information
 *
 **/

module.exports.models = {
    //default model migration strategy, we specify '1' to tell sails
    //that we will handle migration ambiguities manually
    migrate: 'safe'
};
